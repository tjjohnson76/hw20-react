import useLocalStorage from "../lib/hooks/useLocalStorage"



export default function ToDoItemForm(){

  /*
    Collect form data
    Create a new object w/ all that data 
    Add new object to the array of items in state 
    Update local storage
  */

  return (
    <form>
      
    </form>
  )
}